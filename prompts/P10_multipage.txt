<task>
Given the existing GameSpec and a new scanned page, merge them into one
continuous level, preserving player progress where the geometry allows.
Return only the merged GameSpec (the schema has no summary field).
</task>
<constraints>
- Keep existing entity ids stable; append new ones.
- Resolve the seam (how page B connects to page A) as a traversable link.
- Do not restyle or move existing entities beyond the seam join.
</constraints>
