<task>
From the GameSpec palette and the source page, return a BackdropPlan:
ordered background layers (far->near) and the parallax factor for each,
using only colors/textures already present in the drawing. No new artwork
concepts; extend the page's own look.
</task>

<constraints>
1..3 layers. Keep it the child's palette.
</constraints>
