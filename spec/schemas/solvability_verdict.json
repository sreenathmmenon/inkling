{
  "name": "SolvabilityVerdict",
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["verdict", "repairs", "fallback"],
    "properties": {
      "verdict": { "type": "string", "enum": ["ready", "repair", "unsolvable_by_design"] },
      "repairs": {
        "type": ["array", "null"],
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["target_id", "op", "value"],
          "properties": {
            "target_id": { "type": "string" },
            "op": { "type": "string", "enum": ["move", "speed", "gap", "lives"] },
            "value": { "type": "array", "items": { "type": "number" } }
          }
        }
      },
      "fallback": {
        "type": ["string", "null"],
        "enum": ["survive_mode", "none", null]
      }
    }
  },
  "strict": true
}
